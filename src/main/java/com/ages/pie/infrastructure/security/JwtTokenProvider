package com.ages.pie.infrastructure.security;

import org.springframework.stereotype.Component;

/**
 * Exemplo de classe de infrastructure.security.
 *
 * Detalhe técnico de implementação de autenticação, isolado do domínio.
 * O domínio não sabe (e não precisa saber) que existe JWT — ele só lida
 * com o conceito de "usuário autenticado". A geração e validação do token
 * é um detalhe de infraestrutura, substituível sem tocar em domain nem
 * application.
 *
 * Regra para o time: este é um esqueleto para implementação futura da
 * geração/validação real de token (usando uma lib como jjwt). Preencher
 * os métodos abaixo antes de usar em produção — não é código funcional.
 */
@Component
public class JwtTokenProvider {

    public String gerarToken(Long userId, String role) {
        throw new UnsupportedOperationException("Implementar geração de JWT (ex: com jjwt)");
    }

    public boolean validarToken(String token) {
        throw new UnsupportedOperationException("Implementar validação de JWT");
    }

    public Long extrairUserId(String token) {
        throw new UnsupportedOperationException("Implementar extração de claims do JWT");
    }
}